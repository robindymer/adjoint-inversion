# Need to implement mixed FD solver
# Make it general enough to handle both forward and adjoint problem (and their 2nd order versions)
# Start with 2D cartesian domain
# Would need to figure out multiblock SBP-SAT first...

function runForward()
    return nothing
end

function runAdjoint()
    return nothing
end

function computeGradient()
    return nothing
end